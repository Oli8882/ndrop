<?xml version="1.0" encoding="utf-8"?>
<!--
    NDrop — Parking Widget Layout (RemoteViews)
    Signature: Olii-8882
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0E1A"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🅿️  Car Parked"
            android:textColor="#3DFFC0"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Scan to save location"
            android:textColor="#4A5270"
            android:textSize="11sp" />

    </LinearLayout>

    <Button
        android:id="@+id/widget_navigate_btn"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:backgroundTint="#1A2D2A"
        android:text="Navigate"
        android:textColor="#3DFFC0"
        android:textSize="12sp"
        android:visibility="gone" />

</LinearLayout>